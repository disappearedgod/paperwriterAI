- **LLM 交互**：使用 `openai` 和 `anthropic` 库驱动智能体决策与内容生成。
- **量化回测引擎**：集成 `backtrader` 和 `backtesting` 框架，配合 `yfinance`/`akshare` 获取市场数据。
- **AI 检测模型**：本地部署 `transformers` (gpt-neo-2.7B) 运行 Fast-DetectGPT 算法进行文本来源鉴别。