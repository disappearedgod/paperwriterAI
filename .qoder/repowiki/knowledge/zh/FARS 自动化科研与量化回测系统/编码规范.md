- 智能体交互统一采用 JSON 格式作为结构化输出标准，并通过正则表达式提取响应中的 JSON 块进行解析。
- 所有研究产物（论文、代码、数据）均通过 `Workspace` 或 `ResearchArchive` 模块按研究编号（Research ID）进行隔离存储与版本管理。
- 实验执行具备自动重试与自愈机制，当代码执行失败时，Experiment Agent 会捕获错误日志并调用 LLM 进行代码修复后重新运行。